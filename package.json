{
  "name": "wellio",
  "version": "1.0.0",
  "private": true,
  "description": "Journal de santé quotidien — PWA statique, données locales.",
  "scripts": {
    "build": "node build.js",
    "test": "node test/run.js",
    "icons": "python3 make_icons.py"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
